name: projeto_livraria_estrela_branca
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.3.0
  mobx: ^2.6.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mobx_codegen: ^2.7.7
  build_runner: 

flutter:
  uses-material-design: true
  assets:
    - assets/images/